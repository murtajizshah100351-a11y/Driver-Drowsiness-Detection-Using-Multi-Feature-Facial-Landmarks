print('Training MLP...')
mlp = MLPClassifier(hidden_layer_sizes=(64, 32), max_iter=300,
                    early_stopping=True, random_state=SEED)
mlp.fit(feats_tr_n, df_tr['label'])
mlp_prob = mlp.predict_proba(feats_te_n)[:, 1]
res, _ = full_metrics(y_true_test, mlp_prob, 'MLP (HC)')
ALL_RESULTS.append(res); ALL_PROBS['MLP'] = mlp_prob
print(f"  Acc={res['Accuracy']:.4f}  F1={res['F1']:.4f}  AUC={res['AUC-ROC']:.4f}")
