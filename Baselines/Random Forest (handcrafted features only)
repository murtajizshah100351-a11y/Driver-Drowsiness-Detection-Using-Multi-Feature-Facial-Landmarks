print('Training Random Forest...')
rf = RandomForestClassifier(n_estimators=300, max_depth=15,
                            class_weight='balanced', random_state=SEED, n_jobs=-1)
rf.fit(feats_tr_n, df_tr['label'])
rf_prob = rf.predict_proba(feats_te_n)[:, 1]
res, _ = full_metrics(y_true_test, rf_prob, 'Random Forest (HC)')
ALL_RESULTS.append(res); ALL_PROBS['Random Forest'] = rf_prob
print(f"  Acc={res['Accuracy']:.4f}  F1={res['F1']:.4f}  AUC={res['AUC-ROC']:.4f}")
